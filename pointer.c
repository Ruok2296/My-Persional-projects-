#include <stdio.h>

int main() {
    int i =72;
    int*j = &i;int k =65;
    int v = *(&h);
    printf("%d",v);
    printf("the add of i is %u\n",&i);
    printf("the add of i is %u\n",j);
printf("the add of i is %u\n",k);
printf("the addrase of j is %u",&j);

printf("the valude at add j ios is %d",*(&i));
    return 0;
}
