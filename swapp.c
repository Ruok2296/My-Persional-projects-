#include <stdio.h>
void swap(int*x,int*y){
int tem = *n
*x = *y;
*y =tem;

return *x,*y;

}
int main() {
int x = 6,y = 1;
printf("%d\n ",&x);
printf("%d\n ",&y);
swap(&x,&y);

printf("the value of x i %d and the value of y is %d",x,y);
    return 0;
}
