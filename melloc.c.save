#include <stdio.h>
#include<stdlib.h>
int main() {

    int n,i,*ptr,sum =0;
    
    printf("enter the number of elements :");
    scanf("d",&n);
    
    ptr = (int*)malloc(n*sizeof(int));
    
    if(ptr == NULL){
        printf("ERROR!");
        
    }
    
    printf("Enter elements : ");
    for(int i = 0;i<n:++i){
        scanf("%d",ptr +i);
        sum = sum
        
    }

    
    return 0;
}
